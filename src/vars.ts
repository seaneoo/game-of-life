const _PIXEL_SIZE = 16; // The size of cell.
const _WIDTH = 64; // 64 cell rows.
const _HEIGHT = 48; // 48 cell columns.
const _COLOR_ALIVE = "#000000"; // The hex color for an alive cell.
const _COLOR_DEAD = "#ffffff"; // The hex color for a dead cell.
const _CHANCE = 0.1; // The chance for a cell to be alive at the beginning (0.05 = 5%).
const _INTERVAL = 0.5; // The interval that the game runs on, 1 = 1 second

export {
  _PIXEL_SIZE,
  _WIDTH,
  _HEIGHT,
  _COLOR_ALIVE,
  _COLOR_DEAD,
  _CHANCE,
  _INTERVAL,
};
